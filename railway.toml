[build]
builder = "NIXPACKS"
buildCommand = "cd app && pnpm install --frozen-lockfile && pnpm build"
watchPatterns = ["app/**"]

[deploy]
startCommand = "cd app && pnpm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
